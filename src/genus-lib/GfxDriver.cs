namespace genus.lib
{
    public class GfxDriver
    {
        private const float width = 64;
        private const float height = 32;
        private const float frameBufferSize = width * height;

        public GfxDriver()
        {

        }

        public void PrintRawBuffer(byte[] buffer)
        {
            // for (var i = 0; i < width * height)
        }
    }
}